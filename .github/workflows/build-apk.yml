name: Build & Release APK

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.3.1'

      - name: Prepare Keystore and Environment
        run: |
          touch .env
          if [ -f "debug.keystore.base64" ]; then
            base64 -d debug.keystore.base64 > debug.keystore
          elif [ ! -f "debug.keystore" ]; then
            keytool -genkey -v -keystore debug.keystore -alias androiddebugkey -keyalg RSA -keysize 2048 -validity 10000 -storepass android -keypass android -dname "CN=Android Debug,O=Android,C=US"
          fi

      - name: Build Debug APK
        run: gradle :app:assembleDebug --stacktrace --no-daemon

      - name: Rename APK for Release
        run: |
          mkdir -p release-apk
          cp app/build/outputs/apk/debug/*.apk release-apk/QR-Pro-v1.1.1.apk

      - name: Upload Debug APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-v1.1.1
          path: release-apk/*.apk

      - name: Automatically Create or Update Release v1.1.1
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v1.1.1
          name: "QR Pro - Version 1.1.1"
          body: |
            ## 📱 QR Pro - Release v1.1.1
            
            ### ✨ ما تم إنجازه في هذا الإصدار:
            - **إصلاح كامل للأيقونة وتشغيل التطبيق:** دعم فوري ومضمون لجميع شاشات ومطلقات أندرويد (Adaptive Icons & Legacy Mipmaps).
            - **نظام التحديث التلقائي المباشر:** تنزيل وتثبيت التحديثات بضغطة زر من داخل التطبيق دون فتح المتصفح.
            - **استقرار الإقلاع السريع:** حل مشكلة الإغلاق المفاجئ عند فتح التطبيق.
            - **الترقية للإصدار 1.1.1 (versionCode 3).**
            
            📥 **حمل ملف الـ APK المرفق أدناه وثبته على هاتفك مباشرة!**
          files: release-apk/*.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
